{
  "name": "@codeyarn/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for CodeYarn IDE",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@codeyarn/typescript-config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5"
  }
}
